import React from 'react'
import ReactDOM from 'react-dom/client'
import ExamSystem from './app.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <ExamSystem />
    </React.StrictMode>,
)
